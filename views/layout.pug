<!DOCTYPE html>
html(lang="en")
    head
       include parcials/header
    body
        header.barra
            h1 UpTask - Administrador de Proyectos
            a(href="/cerrar-session") Cerrar Session

        main.contenedor
            aside.contenedor-proyectos
                .panel.crear-proyectos
                    a(href="/nuevo-proyecto" class="boton") nuevo Proyecto
                .panel.lista-proyectos
                    h2 Proyectos
                    ul#proyectos.proyectos
                        if(proyectos.length)
                            each proyecto in proyectos
                                li
                                    a(href=`/proyectos/${proyecto.url}`)= proyecto.nombre

            block contenido

        script(src="/dist/bundle.js")
        script(src='https://kit.fontawesome.com/0eb5eff496.js' crossorigin='anonymous')